.env
.env.local
*.local

# Go
backend/bin/
backend/tmp/
*.test
*.out

# Node
node_modules/
web/.next/
# Written by `next dev`/`next build` on every run, and it is generated rather
# than authored — tsconfig.json includes it so the typechecker sees Next's
# ambient types.
web/next-env.d.ts
web/tsconfig.tsbuildinfo
web/playwright-report/
web/test-results/
web/coverage/

# Catalog exports produced by scripts/migrate-from-old-db.sh — real content,
# not fixtures, and large enough that it does not belong in the repository.
songs.ndjson
*.ndjson

# Editors / OS
.DS_Store
.idea/
.vscode/
*.swp

.remember/
